{
    "version": 2,
    "builds": [
        {
            "src": "./url-shortenerProject/server.js",
            "use": "@vercel/node"
        }
    ],
    "routes":[
        {
            "src":"./(.*)",
            "dest":"/"
        }
    ]
}